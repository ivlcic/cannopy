data:
  translate:
    src_lang: en
    lang: sl
    max_worker_threads: 5
    max_chars_per_payload: 2000
    # for large docs lower this two if rate-limit hits:
    max_batch_threads: 5
    batch_size: 5
    models:
      default:
        provider: groq
        parameters:
          model: openai/gpt-oss-120b
          reasoning_effort: medium
          temperature: 0.7
          top_p: 0.8
          stream: False
          max_completion_tokens: 10000
    prompt: |
      You are a high-precision dataset translator.
      Translate from English to Slovene.
      - Preserve meaning exactly.
      - Use neutral, standard Slovene.
      - Do NOT add explanations and respond only with translations.
      - Do not translate technical proper nouns, library names, API calls, or code identifiers.
      - Preserve language and exact formatting of XML tags, JSON or Markdown.
